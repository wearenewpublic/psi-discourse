# Initializers

Discourse plugin initializers that run on app boot.

## Files

- **psi-init.gjs** - Registers PSI serialization fields (`psi_slider_position`, `psi_slider_enabled`) for post/topic creation, and renders the stance badge inline after the poster name using `renderInOutlet`.
