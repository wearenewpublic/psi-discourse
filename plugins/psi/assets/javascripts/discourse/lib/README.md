# Lib

Shared JavaScript constants and utilities for the PSI plugin frontend.

## Files

- **psi-constants.js** - Exports the `SLIDER_POSITIONS` mapping (1-5 to label strings like "Strongly no") and `POSITION_COUNT` (5). Used by slider and results components.
